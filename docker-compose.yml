services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - ./txs.json:/app/txs.json
      - ./notifications.json:/app/notifications.json
      - ./log.txt:/app/log.txt
      - ./error.txt:/app/error.txt
    restart: always
    environment:
      - NODE_ENV=production